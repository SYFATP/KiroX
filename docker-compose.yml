services:
  kirox:
    build: .
    container_name: kirox-web
    restart: unless-stopped
    environment:
      KIROX_WEB_PASSWORD: change-me
    ports:
      - "127.0.0.1:8171:8171"
    volumes:
      - ./data:/data
      - ./output:/output
